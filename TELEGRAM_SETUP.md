# 🚀 Запуск NeverLands в Telegram

## Шаг 1: Создание бота

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду: `/newbot`
4. Введите имя бота (например: `NeverLands Game`)
5. Введите username бота (например: `neverlands_game_bot`)
6. Сохраните **TOKEN**, который даст BotFather

## Шаг 2: Настройка Web App

1. В диалоге с @BotFather отправьте: `/newapp`
2. Выберите вашего бота из списка
3. Введите название приложения: `NeverLands`
4. Введите описание: `Браузерная MMORPG в Telegram`
5. Загрузите иконку 640x360 (или пропустите)
6. Загрузите GIF демо (или пропустите)
7. **ВАЖНО:** На вопрос "Web App URL" введите ваш публичный URL

## Шаг 3: Получение публичного URL (локальное тестирование)

### Вариант A: Ngrok (рекомендуется)
```bash
# Установите ngrok
brew install ngrok

# Запустите туннель на порт 3000
ngrok http 3000

# Скопируйте URL типа: https://abc123.ngrok.io
```

### Вариант B: Localtunnel
```bash
# Установите localtunnel
npm install -g localtunnel

# Запустите туннель
lt --port 3000

# Скопируйте URL
```

### Вариант C: Cloudflare Tunnel
```bash
# Установите cloudflared
brew install cloudflare/cloudflare/cloudflared

# Запустите туннель
cloudflared tunnel --url http://localhost:3000
```

## Шаг 4: Настройка бота

После получения публичного URL:

1. Вернитесь к @BotFather
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Нажмите "Bot Settings" → "Menu Button"
5. Нажмите "Edit Menu Button URL"
6. Введите ваш ngrok URL (например: `https://abc123.ngrok.io`)

## Шаг 5: Обновление конфигурации

1. Откройте файл `backend/config/config.php`
2. Найдите строку с `TELEGRAM_BOT_TOKEN`
3. Вставьте ваш токен от BotFather

## Шаг 6: Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите кнопку MENU (внизу)
3. Выберите "NeverLands"
4. Приложение откроется в Telegram!

---

## 🔧 Troubleshooting

**Проблема:** "This site can't be reached"
- Проверьте, что `npm start` запущен
- Проверьте, что ngrok/localtunnel работает

**Проблема:** "Failed to authenticate"
- Проверьте CORS в `config.php`
- Проверьте API URL в `.env`

**Проблема:** Белый экран
- Откройте DevTools в Telegram Desktop
- Проверьте Console на ошибки
